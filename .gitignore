# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# Node / Next.js
node_modules/
web/.next/
web/out/
web/next-env.d.ts
*.tsbuildinfo

# Env & secrets
.env
.env.local
*.pem

# Local infra volumes
.data/

# Session working notes. Deliberately untracked: it is candid internal state
# (open bugs, what is unverified, what to do next) and the repo is public.
# It stays on disk so a new session can resume from it.
docs/HANDOFF.md

# Fixtures that may contain real customer documents
tests/fixtures/private/

# OS
Thumbs.db
.DS_Store
